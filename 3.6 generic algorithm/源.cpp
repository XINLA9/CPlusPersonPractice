#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
using namespace std;

vector<int> filter(vector<int> ivec, int value, bool(*pred)(int, int)) {
	for (vector<int>::iterator i = ivec.begin(); i != ivec.end(); i++)
	{

	}
}

int main() {
	int ia[10] = {};
	vector<int> ivec(ia, ia + 10);

	return 0;
}


